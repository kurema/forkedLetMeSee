$ apt install ruby ruby-dev eb-utils libeb16-dev git
$ mkdir src
$ cd src
$ git clone https://github.com/kubo/rubyeb19.git
$ cd rubyeb19/
$ ruby extconf.rb
$ make
$ make install
$ apt remove git
$ apt autoremove

COPY

ENABLE CGI

$ chmod 777 /usr/local/apache2/htdocs/*.rb
$ gem install iconv

